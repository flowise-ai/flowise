import { Request, Response, NextFunction } from 'express'
import settingsService from '../../services/settings'

 => {
    try {
        
        
    }  {
        next(e
    }
}

export default {
    getSettingsList
}
