import { Request, Response, NextFunction } from 'express'
import logService from '../../services/log'

// Get logs
 => {
    try {
        
        
    }  {
        next(e
    }
}

export default {
    getLogs
}
