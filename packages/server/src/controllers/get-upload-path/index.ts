import { Request, Response, NextFunction } from 'express'
import { getStoragePath } from 'flowise-components'

 => {
    try {
        const apiResponse = {
            
        }
        
    }  {
        next(e
    }
}

export default {
    getPathForUploads
}
