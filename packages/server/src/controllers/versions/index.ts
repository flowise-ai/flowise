import { Request, Response, NextFunction } from 'express'
import versionsService from '../../services/versions'

 => {
    try {
        
        
    }  {
        next(e
    }
}

export default {
    getVersion
}
