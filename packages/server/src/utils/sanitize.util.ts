exp: any {
    const stack = [obj]

    wh {
        

        ) {
            f {
                const val = current[i]
                 {
                    // eslint-disable-next-line no-control-regex
                    
                } el {
                    
                }
            }
        } el {
            f {
                ) continue
                const val = current[key]
                 {
                    // eslint-disable-next-line no-control-regex
                    
                } el {
                    
                }
            }
        }
    }

    return obj
}
