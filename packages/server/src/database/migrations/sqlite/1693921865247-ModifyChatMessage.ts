import { MigrationInterface, QueryRunner } from 'typeorm'

export class ModifyChatMessage1693921865247 implements MigrationInterface {
    publ: Promise<void> {
        await queryRunner.query(
            `CREATE TABLE "temp_));`
        )
        await queryRunner.query(
            `INSERT INTO "temp_ SELECT "id", "role", "chatflowid", "content", "sourceDocuments", "createdDate" FROM "chat_message";`
        )
        awa
        awa
        awa ;`)
    }

    publ: Promise<void> {
        awa
    }
}
