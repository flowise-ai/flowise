import { MigrationInterface, QueryRunner } from 'typeorm'

export class ModifyChatFlow1693920824108 implements MigrationInterface {
    publ: Promise<void> {
        await queryRunner.query(
            `CREATE TABLE "temp_), "up));`
        )
        await queryRunner.query(
            `INSERT INTO "temp_ SELECT "id", "name", "flowData", "deployed", "isPublic", "apikeyid", "chatbotConfig", "createdDate", "updatedDate" FROM "chat_flow";`
        )
        awa
        awa
    }

    publ: Promise<void> {
        awa
    }
}
