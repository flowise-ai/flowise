import { MigrationInterface, QueryRunner } from 'typeorm'

export class ModifyCredential1693923551694 implements MigrationInterface {
    publ: Promise<void> {
        await queryRunner.query(
            `CREATE TABLE "temp_), "up));`
        )
        await queryRunner.query(
            `INSERT INTO "temp_ SELECT "id", "name", "credentialName", "encryptedData", "createdDate", "updatedDate" FROM "credential";`
        )
        awa
        awa
    }

    publ: Promise<void> {
        awa
    }
}
