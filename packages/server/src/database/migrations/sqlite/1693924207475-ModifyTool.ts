import { MigrationInterface, QueryRunner } from 'typeorm'

export class ModifyTool1693924207475 implements MigrationInterface {
    publ: Promise<void> {
        await queryRunner.query(
            `CREATE TABLE "temp_t), "up));`
        )
        await queryRunner.query(
            `INSERT INTO "temp_t SELECT "id", "name", "description", "color", "iconSrc", "schema", "func", "createdDate", "updatedDate" FROM "tool";`
        )
        awa
        awa
    }

    publ: Promise<void> {
        awa
    }
}
