import { MigrationInterface, QueryRunner } from 'typeorm'

export class AddFileUploadsToChatMessage1701788586491 implements MigrationInterface {
    publ: Promise<void> {
        await queryRunner.query(
            `CREATE TABLE "temp_), ";`
        )
        await queryRunner.query(
            `INSERT INTO "temp_ SELECT "id", "role", "chatflowid", "content", "sourceDocuments", "usedTools", "fileAnnotations", "createdDate", "chatType", "chatId", "memoryType", "sessionId" FROM "chat_message";`
        )
        awa
        awa
        awa ;`)
    }

    publ: Promise<void> {
        awa
        awa
    }
}
