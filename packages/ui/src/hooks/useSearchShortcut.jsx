import { useEffect } from 'react'
import { getOS } from '@/utils/genericHelper'

 === 'macos'

 => {
    u => {
        const component = inputRef.current

         return // Check if inputRef.current is defined

         => {
             || () {
                event.p
                
            }
        }

         => {
             
        }

        
        

         => {
             {
                
            }
            
        }
    },  // Add inputRef to the dependency array to ensure the effect is re-applied if inputRef changes
}

export default useSearchShortcut
